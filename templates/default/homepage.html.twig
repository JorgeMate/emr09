{% extends 'base.html.twig' %}

{% block navbar %}{% endblock %} 

{% block body %}

    <br/>
    <br/>
    <br/>
    
    <br/>
    <br/>
    
    <div class="page-hero d-flex align-items-center justify-content-center">
        {% if app.user.center.slug is defined %}
            <a href="{{ path('user_cpanel', {slug:app.user.center.slug} ) }}"><img src="{{ asset('assets/images/logKS400x200.png') }}"></a>
        {% else %}
            <a href="{{ path('app_login') }}"><img src="{{ asset('assets/images/logKS400x200.png') }}"></a>
        {% endif %}
        
    </div>

    <br/>
    
    <div class="row align-items-center">
        <div class="col-sm-2">
        </div>    
        <div class="col-sm-8 text-center">
            {{ include('default/_flash_messages.html.twig') }}
        </div>
        <div class="col-sm-2">
        </div>    
    </div>

{% endblock %}

{% block footer %}

{% endblock %}
